// Business logic for auth (hashing, token signing, TOTP) goes here.
// Kept separate from the controller so it's independently unit-testable
// and reusable (e.g. called from a CLI seed script too).
export {};
